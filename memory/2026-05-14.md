# 2026-05-14

## Session: Setting up remote node via Tailscale

### Goal
Let Neo (on GCP server) control James' Windows PC remotely.

### Server setup (GCP, Debian 13)
- Gateway was bound to `lan` (0.0.0.0:18789) initially
- Tailscale installed and logged into `ejimm363@gmail.com` tailnet
  - Server Tailscale IP: `100.112.240.25` (`openclaw`)
  - Windows PC Tailscale IP: `100.78.148.5` (`abc`)

### Config changes made
- `gateway.bind` changed: `lan` → `loopback` → `tailnet` → back to `lan`
- `gateway.tailscale.mode` changed: `off` → `serve` → `off`
- Tailscale Serve setup manually: `sudo tailscale serve --bg --https 443 http://127.0.0.1:18789`
  - Gateway URL via Serve: `https://openclaw.tailc6a7a0.ts.net/`
  - Turned off later when switching to direct tailnet bind
- `plugins.entries.device-pair.enabled: true`
- Device pairing config: publicUrl set/unset
- `approvals.exec` enabled with session mode for Telegram approval forwarding

### Node pairing (Windows)
- James ran `openclaw node run/install --host 100.112.240.25` on Windows
- Device `abc` (deviceId: `58e4fb6a...`) paired with `node` role
- Node installed as Windows service via `openclaw node install`
- Device paired successfully and was **connected** at one point (with `system`, `browser`, `file` caps)
- Exec approvals allowlist set: agent `main` → node `abc` → pattern `*`

### Current issue (unsolved)
- After gateway restart, node on Windows won't reconnect
- Device pairing still shows in `devices list` but node status shows `disconnected`
- `openclaw nodes remove --node abc` doesn't work (unknown nodeId)
- Possible cause: node's stored pairing token invalidated after gateway restart
- Need to try: clear `node.json` on Windows and re-install node service

### Red lines established
- NEVER disconnect gateway
- NEVER restart gateway without asking
- Must notify James after any disconnect/reconnect

### Other
- Web search via DuckDuckGo works
- Model: DeepSeek V4 Flash (deepseek/deepseek-v4-flash)
- Key James info: Telegram @josanjames, CS student, FPS gamer, watches American TV, interested in politics
