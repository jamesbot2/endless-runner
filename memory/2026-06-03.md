# 2026-06-03

## GitHub Repo 清理

### 背景
`jamesbot2/subway-surfer` 这个单独的游戏仓库之前被误推了 `neo-creations` 工作区的全部文件（AGENTS.md、SOUL.md、TOML 等），导致两个仓库内容一样。

### 修复操作
- 克隆 subway-surfer 仓库到 `/tmp/subway-surfer-fix`
- 清空所有工作区文件（`git rm -rf .`）
- 从本地 `~/workspace/subway-surfer/` 复制纯游戏文件到根目录
- 添加 `.gitignore`（排除 `node_modules/`、`*.log`、`server/data/users.json`）
- Force push 到 `jamesbot2/subway-surfer` 的 `main` 分支

### 现状
subway-surfer 仓库里只有游戏文件：
- `README.md`
- `game/`（18个游戏模块）
- `server/`（账号服务器 + 静态文件服务器）
- `game.js`、`index.html`、`signin.html`、`game.html`、`style.css`
- `start-servers.sh`

## 服务器安全 — 已完成加固

### 云主机信息
- GCP Debian 13 (trixie)，公网 IP: 35.212.200.85
- Tailscale IP: 100.112.240.25
- 用户: ejimm363

### 防火墙规则
- ✅ 已删 `abc` 规则（原来 0.0.0.0/0 全放行）
- ✅ `allow-game-server` — TCP 3000, 8080 / 来源 0.0.0.0/0 / 优先级 1000
- ✅ `allow-shadowsocks` — TCP+UDP 8388 / 来源 0.0.0.0/0 / 优先级 1000
- ✅ `default-allow-ssh` — TCP 22（保留）
- ❌ 所有其他端口均已封锁
- Dashboard (18789) 不再从公网可达

### SSH
- 密钥登录 ✅ / 禁 root ✅ / 自动安全更新 ✅

### 翻墙 — Shadowsocks
- 端口 8388 (TCP+UDP)
- 加密: chacha20-ietf-poly1305
- 密码: rKbJDTiZZWM3PzVVQvNdQ==
- systemd 服务已启用，开机自启
- Clash Verge 客户端已配好（本地配置文件）

### OpenClaw Dashboard
- 只能通过 Tailscale 访问: http://100.112.240.25:18789
- `allowInsecureAuth=true` 仍开启（待加固）
- 当前版本低于 2026.5.28，有更新可用

### SubwaySurfer 游戏服务
- 账号服务器: :3000（运行中）
- 静态文件服务器: :8080（运行中）
- 本地路径: ~/.openclaw/workspace/subway-surfer/

## SubwaySurfer 功能增强 — 排行榜 + 祖国人模式修正

### 新增：排行榜
- 主菜单新增 🏆 LEADERBOARD 按钮
- 显示所有已验证用户在 Easy/Medium/Hard 三个难度下的最远距离
- 每列距离旁显示该记录当时装备的技能（None / Double Jump / Jetpack / Roof Walk）
- 排行榜数据来自 `/api/leaderboard`，邮箱已脱敏

### 修改：祖国人模式
- 祖国人模式下跑的距离、获得的金币、credit**不再计入**记录
- `gameOver` 中加了 `if (!SG.state.homelander)` 守卫：不更新 per-difficulty 最远距离、不保存金币/credit
- `quitToMenu` 中增加了 `deactivateHomelander()` 调用，回到主菜单自动切回普通模式
- 刷新页面相当于重新加载游戏，自然回到普通模式
- `activateHomelander()` 中已有的 `legitRun = false` 继续生效

### 技术改动
- `game/state.js` — 新增 `maxEasyAbility`、`maxMediumAbility`、`maxHardAbility` 字段
- `game/main.js` — homelander 守卫 + quitToMenu/restartGame 退出祖国人
- `game/account.js` — 排行榜 UI + 能力数据同步 + profile 显示装备技能
- `game/ui.js` — 主菜单排行榜按钮
- `server/account-server.js` — 增强 leaderboard API，保存/读取能力数据
- `game.js` — 已重新合并所有模块
